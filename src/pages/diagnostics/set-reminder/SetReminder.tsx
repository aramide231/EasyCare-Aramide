import ReminderPage from "@/pages/frontdesk/set-reminder/ReminderPage";

/** Laboratory Set Reminder — same UI as Front Desk. */
const SetReminder = () => {
  return <ReminderPage />;
};

export default SetReminder;
